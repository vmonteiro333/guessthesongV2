import type { Playlist } from "../../types/playlist";

// GERADO AUTOMATICAMENTE pelos scripts de importação — evite editar à mão.

import { playlist as playlist_playlist_bala } from "./playlist-bala";
import { playlist as playlist_principal } from "./principal";

export const playlists: Playlist[] = [
  playlist_playlist_bala,
  playlist_principal,
];
